void setup() {
  pinMode(0, Input);
  pinMode(1, Input);
  pinMode(2, Input);
  pinMode(3, Input);
  pinMode(4, Input);
  pinMode(5, Input);
  pinMode(6, Input);
  pinMode(7, Input);
  pinMode(10, Input);
  pinMode(11, Input);

}


void loop() {
  float time;

  
  time -= timeDecrement;
}
